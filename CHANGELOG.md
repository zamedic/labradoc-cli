# Changelog

## [v1.0.2] - 2026-03-17

### Fixed
- Write error bodies to stderr instead of stdout for AI agent compatibility

### Changed
- Rename `API_TOKEN` env var to `LABRADOC_API_TOKEN` for app uniqueness
- Rename CLI binary use from `labradoc` to `labradoc-cli` in root command definition

### Improved
- Validate `--status` values for `files list` command with defined allowed options
- Recommend API token auth via labradoc.eu/profile as preferred authentication method

### Documentation
- Improve `SKILL.md` for clawhub publishing

## [v1.0.1] - Initial release
